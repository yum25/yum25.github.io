{
  "name": "yum25.github.io",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "preprocess": "node build.js",
    "dev": "pnpm preprocess && vite",
    "build": "pnpm preprocess && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "nunjucks": "^3.2.4",
    "sharp": "^0.35.2",
    "vite": "^6.0.5"
  }
}
